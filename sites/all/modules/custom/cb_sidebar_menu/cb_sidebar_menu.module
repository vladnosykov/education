<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function cb_sidebar_menu_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'ctools') {
    return 'plugins/' . $plugin_type;
  }
}